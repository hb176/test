package com.example.service;

import com.example.dto.User;

import java.util.List;

public interface UserService {
    public Integer insertUser(User user);
   // List<User> getAllUserInfo(int pageNum, int pageSize);
}
